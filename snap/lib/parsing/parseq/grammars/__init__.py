
def build(ENV):

	class ParseqGrammarsModule(object):

		__slots__ = []

		# TODO __getattr__() does build and assign locally of the chosen grammar...  (the way parsing.language does)


	raise NotImplementedError('should be ENV.parsing.parseq...')
	ENV.parseq.grammars = ParseqGrammarsModule()
