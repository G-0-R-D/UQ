
#ifndef __SNAP_ENV_H__
#define __SNAP_ENV_H__

#include "SnapObject.h"

/* TODO make SnapEnv object api*/

/* TODO ENV could do lazy loading per-element (because compiler would know where each element is and can encode it at compile time!)
	__getattr__ of ENV to lazy load? */


/* TODO also make SnapEnv_push_stack(), SnapEnv_pop_stack() calls for ops to use? */

#endif /* __SNAP_ENV_H__ */
